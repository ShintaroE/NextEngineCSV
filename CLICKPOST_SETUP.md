# クリックポスト自動決済機能 セットアップガイド

## 📋 概要

このアプリケーションは、ネクストエンジンから出力したCSVデータを使用して、クリックポストの決済処理を自動化します。

## 🚀 初回セットアップ

### 1. 依存関係のインストール

```bash
npm install
```

Playwrightが自動的にインストールされ、必要なブラウザバイナリもダウンロードされます。

### 2. セレクターの調整（重要）

**このステップは必須です！** クリックポストのサイト構造に合わせてセレクターを調整する必要があります。

#### 手順:

1. **アプリケーションを起動**
   ```bash
   npm start
   ```

2. **クリックポストサイトを手動で確認**
   - ブラウザで https://clickpost.jp/ にアクセス
   - ログイン後、「新規登録」ページに移動
   - **F12キー**を押して開発者ツールを開く

3. **フォーム要素のセレクターを確認**

   **例: 郵便番号フィールド**
   - 開発者ツールの「要素選択ツール」（矢印アイコン）をクリック
   - 郵便番号入力欄をクリック
   - 右側のHTMLで以下を確認:
     ```html
     <input name="postal_code" id="postal" type="text">
     ```
   - このフィールドは `input[name='postal_code']` または `#postal` でアクセス可能

   **その他のフィールドも同様に確認:**
   - お届け先氏名
   - 住所1, 2, 3
   - 内容品
   - 決済ボタン

4. **clickpost-selectors.jsonを編集**

   プロジェクトルートの `clickpost-selectors.json` を開き、実際のセレクターに更新します:

   ```json
   {
     "labelForm": {
       "postalCode": "input[name='postal_code']",  // ← 実際のname属性
       "recipientName": "input[name='recipient_name']",  // ← 実際のname属性
       "address1": "textarea[name='address1']",
       "address2": "textarea[name='address2']",
       "address3": "textarea[name='address3']",
       "content": "input[name='content']",
       "submitButton": "button[type='submit'].btn-primary"  // ← クラス名も確認
     }
   }
   ```

   **ヒント:** 複数のセレクターをカンマ区切りで指定すると、フォールバック対応になります:
   ```json
   "postalCode": "input[name='postal_code'], #postal, input[id*='postal']"
   ```

## 🎯 使い方

### 1. CSVの準備

ネクストエンジンの検索機能でCSVを出力します。CSVには以下の列が必要です:

- `お届け先郵便番号` または `郵便番号`
- `お届け先氏名` または `氏名`
- `お届け先住所1` または `住所1`
- `お届け先住所2` または `住所2`
- `お届け先住所3` または `住所3`（オプション）
- `商品名`

### 2. 自動決済の実行

1. アプリケーションの「**クリックポスト自動決済**」タブを開く
2. 「**CSVファイルを開く**」ボタンをクリックしてCSVを選択
3. データのプレビューが表示されます（最初の10件）
4. 「**自動決済を開始**」ボタンをクリック
5. ブラウザが自動的に起動します
6. **手動でYahoo!ウォレットまたはAmazon Payにログイン**
7. ログイン完了後、自動的に処理が開始されます
8. 進行状況がリアルタイムで表示されます

### 3. 処理の停止

処理中に「**停止**」ボタンをクリックすると、現在の処理が完了後に停止します。

## ⚠️ 注意事項

### セキュリティ

- **ログイン情報は保存されません**。毎回手動でログインが必要です
- ブラウザは`headless: false`（表示モード）で起動するため、処理を目視確認できます

### レート制限

- 1件の処理ごとに**1秒の待機時間**を設けています
- 連続リクエストによるアカウントBANを防ぐための措置です
- 必要に応じて[clickpost-automation.js:82](clickpost-automation.js#L82)の`waitForTimeout`を調整できます

### エラーハンドリング

- エラーが発生した行はスキップして次に進みます
- エラー内容は進行状況ログに表示されます
- 処理完了後、エラーログを確認してください

### 最初は少量データでテスト

- **最初は1-2件のCSVでテスト**してください
- セレクターが正しく動作することを確認してから、大量データを処理してください

## 🔧 トラブルシューティング

### 問題: フォームに入力されない

**原因:** セレクターが間違っている

**解決策:**
1. 開発者ツール（F12）でフォーム要素を確認
2. `clickpost-selectors.json`のセレクターを実際のものに更新
3. アプリを再起動

### 問題: 「要素が見つかりません」エラー

**原因:** ページ読み込みが遅い、またはセレクターが間違っている

**解決策:**
1. セレクターを確認
2. [clickpost-automation.js:183](clickpost-automation.js#L183)の`timeout: 3000`を増やす（例: `timeout: 5000`）

### 問題: ログインタイムアウト

**原因:** 5分以内にログインが完了しなかった

**解決策:**
1. [clickpost-automation.js:65](clickpost-automation.js#L65)の`timeout: 300000`（5分）を増やす（例: `600000`で10分）
2. ログイン情報を事前に準備しておく

### 問題: 決済ボタンがクリックされない

**原因:** 決済ボタンのセレクターが間違っている

**解決策:**
1. 開発者ツールで決済ボタンの`class`や`id`を確認
2. `clickpost-selectors.json`の`submitButton`を更新:
   ```json
   "submitButton": "button.確認したクラス名, input[type='submit']"
   ```

## 📚 参考情報

### セレクター情報源

クリックポストの自動化については、以下の記事が参考になります:

- [クリックポストで連続自動支払いしてみた - Qiita](https://qiita.com/yu_bookstore/items/a599930b7fde7e53f429)
- [クリックポスト拡張機能 by GoQSystem](https://goqsystem.com/clickpost/)

### セレクターの書き方

- `#id名` - IDで指定
- `.class名` - クラス名で指定
- `input[name='名前']` - name属性で指定
- `button[type='submit']` - 属性で指定
- `div > button` - 親子関係で指定

### 複数セレクターの指定

フォールバック対応のため、複数のセレクターをカンマ区切りで指定できます:

```json
"postalCode": "input[name='postal_code'], #postal, input[id*='postal']"
```

左から順に試され、最初に見つかった要素が使用されます。

## 🆘 サポート

問題が解決しない場合は、以下の情報を添えてお問い合わせください:

1. エラーメッセージ
2. 処理中のスクリーンショット
3. 使用しているCSVのサンプル（個人情報は削除）
4. `clickpost-selectors.json`の内容
